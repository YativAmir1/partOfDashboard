# דוח רכיבי מערכת - Ramat Gan AI Dashboard

## מטרת הפרויקט

הפרויקט הוא דמו פרונטאנד בלבד לדשבורד עירוני סינתטי מבוסס "AI" עבור עיריית רמת גן. הוא משתמש בנתוני JSON/TypeScript מקומיים, טיימרים דטרמיניסטיים ו-React Context כדי להציג סיפור קצר של 1-2 דקות: זיהוי חריגה, קורלציית AI, המלצה, שיגור משימה, עדכון תושב, שיפור KPI וסגירת האירוע.

אין בפרויקט Backend, בסיס נתונים, אימות משתמשים או אינטגרציית AI אמיתית. התנהגות ה-"AI" מתוסרטת דרך `DemoContext`, ובמקביל `HazardContext` נפרד חושף אירוע מצלמה/תאונת דרכים לאחר 30 שניות.

## נתיבי האפליקציה

### `app/layout.tsx`
מגדיר את מעטפת האפליקציה הגלובלית בעברית מימין לשמאל, טוען את פונט Heebo, מפעיל סקריפט ראשוני למצב כהה, ועוטף את כל העמודים ב-`HazardProvider` וב-`DemoProvider`. בנוסף הוא מציג את הסיידבר, ההדר, באנר התראת התרחיש, אזור התוכן הראשי וטוסט הסכנה.

### `app/page.tsx`
מפנה את נתיב השורש אל `/overview`. כך מסך תמונת המצב העירונית הוא מסך הפתיחה הקבוע של הדמו.

### `app/overview/page.tsx`
בונה את מסך תמונת המצב למנהלים, כולל כרטיסי KPI, מד בריאות עירוני, התראות קריטיות, מזג אוויר, אירועים מיוחדים, קטגוריות פנייה מובילות ואשכולות תלונות חוזרות. המסך מאפשר להריץ או לאפס את התרחיש ומגיב לשלבי התרחיש באמצעות סטטוס קורלציה, ציון סיכון, מקורות זיהוי ו-KPI מונפשים.

### `app/map/page.tsx`
בונה את מסך מפת העיר עם סינון לפי קטגוריות אירועים ושכבות מידע כמו פחים חכמים, מצלמות, צוותים, רכבים ואזורי סיכון. בזמן תרחיש פעיל הוא מדגיש את מרום נווה ומציג פאנל סיכון; במצב רגיל הוא מציג דירוג עומסים לפי רובע.

### `app/operations/page.tsx`
בונה את מסך התפעול לטיפול באירועים, שיגור משימות שטח, ניתוח משפך פניות, תרשימי זמני תגובה ובלוקים של החלטת AI. סינון הרובע נשמר מקומית בעמוד ומועבר אל `useRequestFunnel`.

### `app/citizens/page.tsx`
בונה את מסך השירות לתושב עם אנליטיקת ערוצי פנייה, התנהגות מוקד 106/109, מגמות תלונות, עמידה ב-SLA, חלוקה לקטגוריות, סנטימנט ורשימת פניות אחרונות. סינון ערוץ התקשורת נשמר מקומית ומועבר לתרשים הדונאט ולתרשים יעילות הערוצים.

### `app/employees/page.tsx`
בונה את מסך עובדי העירייה מתוך `EMPLOYEES_DATA`, כולל כרטיסי מחלקות, שורות סטטוס צוותים, סיכומי KPI ולוחות שיבוץ שבועיים נפתחים. המסך מדגים זמינות כוח אדם וכיסוי רובעים כחלק מסיפור תפעול השטח.

### `app/insights/page.tsx`
בונה את מסך תובנות ה-AI, שהוא נקודת הכניסה המרכזית להרצת התרחיש. הוא מציג כפתור הרצת תרחיש או פאנל תרחיש פעיל, ולאחר מכן המלצות, יומן חריגות, מדדי יעילות וכרטיסי SLA.

### `app/cameras/page.tsx`
מציג את מסך המצלמות החכמות באמצעות `CameraGrid`. זהו עמוד דק שמעטפתו היחידה היא רכיב דשבורד המצלמות.

## רכיבי Layout

### `Header`
מציג את הפס העליון עם כותרת המערכת, תאריך ושעה לפי שעון ירושלים, לוגו, כפתור החלפת ערכת נושא ואינדיקציה האם התרחיש פעיל. זמן ותאריך חיים מחושבים בתוך `useEffect` כדי למנוע בעיות Hydration.

### `Sidebar`
מציג תפריט ניווט קבוע עם קישורים למסכים, אייקונים, מיתוג והדגשת הנתיב הפעיל. הוא מספק את מבנה הניווט התפעולי המרכזי של האפליקציה.

### `AlertBanner`
מציג באנר התראה אדום וניתן לסגירה כאשר `DemoContext` חושף התראה פעילה. הוא קשור לשלבי התרחיש המתוסרטים ונעלם לאחר סגירה ידנית או בסיום התרחיש.

### `HazardToast`
מציג טוסט חירום בפינה השמאלית התחתונה לאחר ש-`HazardContext` חושף סכנה אחרי 30 שניות. הוא נסגר אוטומטית לאחר 9 שניות, תומך בסגירה ידנית ומקשר למסך `/cameras`.

### `ThemeToggle`
מחליף את מחלקת `dark-mode` על אלמנט השורש ושומר את הבחירה ב-`localStorage`. הרכיב הוא Client-only כי הוא נוגע באחסון דפדפן וב-DOM.

## רכיבי Overview

### `CityHealthGauge`
מציג מד חצי-עגול לציון הבריאות העירונית הנוכחי. הוא מנפיש שינויי ציון באמצעות `useCountUp` ומשנה צבע ותווית לפי טווחי ציון.

### `CriticalAlertsSection`
מציג אירועים קריטיים מתוך נתוני האירועים הסטטיים ומסתיר את `INC-ACC-001` עד ש-`hazardRevealed` הופך ל-true. כל התראה מקשרת למסך התפעול ומשתמשת באייקונים, צבעים, סטטוס ותוויות רובע לפי קטגוריה.

### `KpiCard`
מציג ערך KPI, יחידה אופציונלית, שינוי ביחס לבסיס, אייקון ו-Sparkline. הוא מנפיש מספרים ותומך בלוגיקת Delta הפוכה למדדים שבהם ערך נמוך יותר הוא טוב יותר.

### `WeatherWidget`
מציג תחזית קשיחה לשבעה ימים ברמת גן עבור שבוע הדמו. הוא כולל הדגשת היום הנוכחי, טמפרטורות גבוה/נמוך, אייקוני מזג אוויר וסימון אירוע מיוחד.

### `SpecialOccasionsWidget`
מציג אירועים עירוניים/חגים קשיחים לאותו שבוע ואת ההשפעות התפעוליות שלהם. הוא מחבר את פעילות הדשבורד לתנאים עירוניים צפויים.

## רכיבי Map

### `LayerFilter`
מספק בקרות מפה לקטגוריות אירועים ולשכבות מידע כמו פחים חכמים, מצלמות, צוותים, רכבים ואזורי סיכון. הוא מקבל מהעמוד את מצב הבחירה ואת פונקציות ההפעלה/כיבוי.

### `CityMap`
מציג את מפת Leaflet ואת כל שכבות הדמו הגיאוגרפיות. הוא מציג סמני אירועים, פחים חכמים, מצלמות CCTV, צוותי עובדים, רכבים, סמני קפיצה של התרחיש, מעגלי סיכון, תנועת צוותים מונפשת וסמן סכנה שמופיע אחרי 30 שניות.

### `MapLegend`
מציג מקרא פשוט לצבעי קטגוריות האירועים ולשקיפות לפי סטטוס. הוא זמין ככלי עזר למפה, אם כי העמוד הנוכחי משתמש בעיקר ב-`LayerFilter`.

## רכיבי Operations

### `AiDetectionBlock`
מציג במצב רגיל חריגות מתוך `anomalies.json`, או בזמן תרחיש את נרטיב זיהוי ה-AI הפעיל. במהלך התרחיש הוא מציג סיכון, מקורות זיהוי, תיאור חריגה חי ונקודות ניתוח.

### `SystemDecisionBlock`
מציג במצב רגיל המלצות ממתינות מתוך הנתונים הסטטיים, או בזמן תרחיש את ההמלצה הפעילה ואת ציר שיגור המשימה. הוא מחבר בין סיפור ההחלטה לבין צוותי עובדים זמינים וסטטוס משימה לפי שלב.

### `EventTicker`
מציג פיד תפעולי שנראה חי מתוך `useLiveTicker`. סדר האירועים דטרמיניסטי, ואירוע חדש מתווסף כל 9 שניות.

### `IncidentTable`
מציג טבלת אירועים ניתנת למיון ולסינון עם תגיות עדיפות/סטטוס, אייקוני מקור, אחראים, תיאורים ותוויות רובע. גם כאן `INC-ACC-001` מוסתר עד שטיימר הסכנה חושף אותו.

### `ResponseTimeChart`
מציג תרשים עמודות של זמני תגובה לפי מחלקה עם קו ייחוס SLA. צבעי העמודות מדגישים מחלקות מעל או מתחת לספי תגובה.

### `TaskPanel`
מציג את תור שיגור משימות השטח. כאשר התרחיש יוצר משימה, הוא מחדיר את המשימה שנוצרה על ידי AI לראש הרשימה ומשנה את הסטטוס המוצג שלה לפי שלב התרחיש.

## רכיבי שירות לתושב

### `ComplaintCategoryChart`
מאגד פניות לפי קטגוריה ומציג את הקטגוריות המובילות כתרשים עמודות אופקי של Recharts. הוא מאזין למחלקת `dark-mode` בשורש כדי להתאים תוויות ו-tooltips למצב כהה.

### `ComplaintVolumeChart`
מציג נפח פניות ב-30 יום כתרשים שטח עם קווי ייחוס להידרדרות ולהפעלת AI. בזמן התרחיש הוא מוסיף נקודת "עכשיו" חיה שמשתפרת יחד עם התאוששות ה-KPI.

### `Contact106Module`
מדמה מוקד 109/106 חכם עם מדדי הסטת עומס שמשתנים לפי שלב ודוגמאות לפניות SLA. הוא מפריד בין פניות בתוך SLA לבין פניות בחריגה ומשנה תגיות ומדדים לפי התקדמות התרחיש.

### `RecentComplaintsList`
מציג את עשר הפניות האחרונות מתוך נתוני התלונות המקומיים. כל שורה כוללת סנטימנט, קטגוריה, רובע, תאריך, תיאור וסטטוס.

### `SatisfactionTrendChart`
מציג את 14 הימים האחרונים של שביעות רצון ועמידה ב-SLA כשתי סדרות קו. הוא כולל קו יעד של 90%.

### `SlaGauge`
מציג עמידה נוכחית ב-SLA כמד עגול שמונע ממצב ה-KPI של `DemoContext`. הוא משנה צבע ותווית לפי מצב: ביעד, בסיכון או בחריגה.

### `TopComplaintsWidget`
מדרג אשכולות של תלונות חוזרות מתוך `complaintClusters.json`. במצב קומפקטי הוא מציג רק בעיה וכמות, ובמצב מלא הוא מציג גם מיקום, קטגוריה, פס התקדמות וסטטוס.

## רכיבי אנליטיקה בדשבורד

### `ChannelDonutChart`
מציג פניות לפי ערוץ תקשורת כתרשים דונאט אינטראקטיבי. לחיצה על פלח מפעילה או מנקה סינון ערוץ ומשנה את טקסט המרכז ואת שקיפות הפלחים.

### `RequestFunnelChart`
מציג את משפך מחזור חיי הפנייה כתרשים עמודות. הוא מתייג כל שלב, צובע שלבים לפי סטטוס ומחשב אחוזי נשירה בין שלבים.

### `TopCategoriesWidget`
מציג קטגוריות פנייה מובילות כפסים אופקיים לחיצים. הקטגוריה הנבחרת מודגשת וניתן לנקות אותה בלחיצה חוזרת או בכפתור ניקוי.

### `ChannelEfficiencyChart`
מציג שיעורי פתרון והסלמה לפי ערוץ תקשורת כעמודות מקובצות. הוא קורא נתונים דרך `useChannelEfficiency` ויכול לצמצם את התצוגה לערוץ אחד שנבחר.

### `FieldPerformanceCard`
מסכם ביצועי שטח באמצעות שיעורים מתוך `useFieldStats`. הוא משווה בין פניות ששויכו לשטח לבין פניות שטופלו בשטח ומדגיש את הפער.

### `SlaMetricsCards`
מציג שלושה כרטיסי SLA תפעוליים: זמן תגובה ממוצע, זמן פתרון ממוצע ושיעור חריגות SLA. הערכים מגיעים ישירות מ-`operationalMetrics.json`.

## רכיבי Insights

### `AnomalyLog`
מציג רשימת חריגות מהנתונים הסטטיים ומוסיף לראש הרשימה את חריגת התרחיש החיה כאשר היא קיימת. הוא מציג חומרה, רובע, סוג חריגה, שעה ומצב פתוח/טופל.

### `EfficiencyMetrics`
מציג מדדי השפעת AI כמו שעות עבודה שנחסכו, פניות שטופלו אוטומטית, דקות תגובה שנחסכו והמלצות שהופקו. הערכים גדלים כאשר מדדי התרחיש משתפרים.

### `RecommendationCard`
מציג המלצת AI עם עדיפות, קטגוריה, רובע, תיאור, פס ודאות והשפעה צפויה. הוא מסמן ויזואלית את המלצת התרחיש שהוזרקה כאשר היא קיימת.

### `RunScenarioButton`
מספק את כפתור הפעולה המרכזי להפעלת תרחיש ה-AI המתוסרט. הוא קורא ל-`runScenario` מתוך `DemoContext`.

### `ScenarioPanel`
מציג את ציר התרחיש הפעיל, ציון הסיכון, אזור פעיל, מקורות זיהוי, שרשרת נימוק, כפתור איפוס וטבלת KPI לפני/אחרי בסיום. זהו הרכיב שמסביר בצורה המקיפה ביותר את מנוע התרחיש בן 60 השניות.

## רכיבי Cameras

### `CameraGrid`
מציג את דשבורד המצלמות החכמות מתוך נתוני מצלמות מקומיים ומצב התרחיש. מצלמות מרום נווה עוברות למצב התראה בשלבים פעילים, עוברות להודעת סיום לאחר פתרון, וכל כרטיס כולל וידאו/שידור, שכבות CCTV, שעון חי ופוטר אירוע.

## רכיבים משותפים

### `PriorityBadge`
ממפה מחרוזות עדיפות לתוויות עבריות ולעיצוב תג צבעוני. הוא משמש בהמלצות, משימות וממשקי אירועים.

### `SentimentIcon`
מציג נקודה צבעונית קטנה עבור סנטימנט חיובי, ניטרלי או שלילי. הוא משמש ברשימת הפניות האחרונות.

### `SparklineChart`
עוטף `LineChart` מינימלי של Recharts עבור קווי מגמה קטנים בתוך רכיבים. הוא משמש בכרטיסי KPI.

### `StatusBadge`
ממפה מחרוזות סטטוס של אירועים/משימות לתוויות עבריות, נקודות צבעוניות ועיצוב Pill. הוא מרכז את תצוגת הסטטוס עבור אירועים, משימות, תלונות והתראות.

## Context Providers

### `DemoContext`
מחזיק את מנוע התרחיש הדטרמיניסטי בן 60 השניות ואת מצב ה-KPI הנוכחי. הוא מתזמן מעברי שלבים, מזריק המלצה/משימה/חריגה לדמו, מנפיש שינוי KPI ב-20 צעדים וחושף נתוני תרחיש מחושבים לכל המסכים דרך `useDemo`.

### `HazardContext`
מחזיק את מצב חשיפת הסכנה העצמאי לאחר 30 שניות. הדגל `hazardRevealed` מפעיל את אירוע התאונה הקריטי המוסתר, את טוסט הסכנה ואת סמן המפה, ללא תלות בתרחיש הראשי.

## Hooks

### `useChannelEfficiency`
קורא מדדי יעילות ערוצים מתוך `operationalMetrics.json` ומסנן אופציונלית לפי ערוץ שנבחר. הוא מחזיר נתונים מוכנים לתרשים של פתרון מול הסלמה.

### `useContactMetrics`
קורא מדדי מוקד מתוך `contactMetrics.json`. הוא מחזיר סך פניות, נתוני ערוצי פנייה ממוינים ושיעור פניות מידע.

### `useCountUp`
מנפיש מעבר מספרי מהערך הקודם לערך יעד באמצעות `requestAnimationFrame`. הוא משמש במדדים, כרטיסי KPI ותיבות מדד.

### `useDistrictLoad`
מאגד תלונות לפי רובע ומסנן אופציונלית לפי קטגוריה/סטטוס. הוא מחזיר עומס רובעים ממוין ואת הרובע בעל העומס הגבוה ביותר.

### `useFieldStats`
גוזר שיעורי שיוך לשטח וטיפול בשטח מתוך משפך הפניות. הוא תומך בכרטיס ביצועי השטח.

### `useLiveTicker`
יוצר טיקר תפעולי חי אך דטרמיניסטי. הוא מתחיל מזמני Placeholder יציבים למניעת בעיות Hydration, ולאחר Mount מעדכן זמנים ומוסיף אירועים באינטרוול.

### `useRequestFunnel`
מחזיר את משפך הפניות המוגדר כברירת מחדל מתוך מדדי המוקד או מחשב משפך ייעודי לרובע מתוך נתוני האירועים. הוא תומך בסינון הרובע במסך התפעול.

### `useSlaMetrics`
מחזיר מדדי SLA ישירות מתוך `operationalMetrics.json`. הוא פשוט בכוונה, כי האפליקציה משתמשת בנתוני דמו סטטיים.

### `useTopCategories`
מאגד קטגוריות תלונה בכל העיר או לפי רובע שנבחר. הוא מחזיר ספירות ואחוזים של קטגוריות מובילות עבור אנליטיקת ה-overview.

## מודולי נתונים וכלים

### `lib/data.ts`
מרכז ייבוא וייצוא טיפוסי של כל נתוני ה-JSON המקומיים שבהם הדשבורד משתמש. בנוסף הוא מגדיר ערכי KPI לפני/אחרי ומפות צבעים/תוויות משותפות לקטגוריות, עדיפויות וסטטוסים.

### `lib/hebrew.ts`
מרכז תוויות עבריות ופונקציות עזר לרובעים, עדיפויות, סטטוסים, קטגוריות פנייה וסוגי חריגות. הוא מאפשר ל-UI לשמור מפתחות דומיין יציבים באנגלית ולהציג טקסט עברי מקומי.

### `lib/types.ts`
מגדיר טיפוסי TypeScript משותפים לאירועים, סמני מפה, משימות, תלונות, KPI, שלבי תרחיש, מצלמות, פילטרים, מדדים ונתוני ניהול עובדים. זהו החוזה בין הנתונים המקומיים, ה-hooks, ה-context וה-UI.

### `lib/utils.ts`
מייצא את העזר `cn`, שמחבר `clsx` ו-`tailwind-merge` להרכבת מחלקות Tailwind מותנות בצורה בטוחה. הוא נמצא בשימוש ברכיבי תגיות ו-layout רבים.

### `data/*.json`
מכיל את כל נתוני התפעול הסינתטיים: אירועים, סמני מפה, מגמות, המלצות, חריגות, משימות, מחלקות, תלונות, אשכולות תלונה, מצלמות, מדדי מוקד ומדדים תפעוליים. הקבצים האלה מחליפים כל צורך ב-Backend או API בדמו.

### `data/employeesData.ts`
מכיל נתוני מחלקות, צוותים, לוחות שיבוץ, אחראים, רכבים, סטטוסים, כיסוי רובעים ומשימות פתוחות. הוא מפעיל את מסך העובדים ואת שכבות צוותי העובדים במפה.

### `public/cameras/*`
מכיל נכסי וידאו מקומיים עבור כרטיסי המצלמות. הקבצים מאפשרים למסך המצלמות להרגיש חי ועדיין לעבוד כנכס דמו אופליין.
